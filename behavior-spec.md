# dsh-auto-collapse 0.2 行为规范

本规范依据用户 2026-09-07 的明确要求：工作过程中，两个正文之间的思考、工具、上下文合成一个二级；原生一级展开后仍保留二级。它替代旧版“自建一级 + 分类二级 + 三级全文复制”规则。

## 层级

1. 原生一级负责完成回合的整体折叠、计数、正文保留范围。插件不自建一级，不计算时长，不判定 final。
2. 插件二级负责正文之间的混合工作段。非空段默认收起，单条工作也成段；完成后仍保留。
3. 二级展开后显示该段的原生思考/工具/上下文（含系统提示词）/已识别命令行。三级详情由宿主管理，不自动点击或复制全文。

## 分组

- 同一个正文间隔内，思考、工具、上下文及已支持的命令类型统一合并；类型变化不是边界。
- 正文、用户消息、steering、不同 turn 和未知语义节点是边界。Markdown 换行与段落不机械拆组。
- 一个 assistant-step 内出现“思考—正文—思考”时，以正文切成不同工作段。不能以一消息一 chip 限制分组。
- 纯图片、SVG、canvas 等正文也构成边界；不修改正文或承载正文的共同祖先。
- 首段正文前、末段正文后以及等待下一段正文的工作均可成段；不生成空摘要。
- 无 turn 属性的上下文只在相邻归属明确且不跨用户边界时继承 turn；歧义范围保持分离。

## 状态与展示

- 流式新增工作加入当前段；新正文结束当前段，后续工作新建一段。
- 运行摘要显示当前动作及原生摘要，完成摘要概括实际动作类型；错误/停止/等待有明确状态。
- 二级展开意图由稳定的正文边界和成员身份保持，不跟随 running 项变化重置。
- 二级展开只露出原生工作行，保留三级详情状态。思考全文只由原生组件提供。
- 原生一级暂时收起时，隐藏其实际控制范围内的二级展示并保留状态；重开后恢复二级，不跳过二级直接展开三级。
- 工作段有原生范围外的上下文时，二级放在首个仍可见的成员位置，保留可访问入口。用户展开二级时可按需打开原生一级。
- `system-prompt` 作为上下文加入相邻工作段，不另作分组边界。DSH 将其排除在原生过程成员外；有明确 `data-chat-turn` 且对应一级存在时，插件补充隐藏，使其跟随该一级收起，即使二级此前展开也不会露出。一级重开后恢复二级与原生详情状态；无明确 turn 或无一级时保留二级入口。
- Normal 模式没有原生一级时，二级仍按工作段显示；不自动修改宿主设置。

## 内容与可达性

- 原生节点保持原父子关系；只插入插件摘要和修改登记过的工作项隐藏属性。
- 只隐藏已证明仅承载工作内容的节点/祖先，不遍历任意空 div，不改变内联 display 及其 priority。
- 使用 hidden=until-found 保持可搜索性；beforematch 按需打开一级与二级。
- 焦点、原生选中和等待输入的工作保持可达；用户需要操作的控件不会被自动藏起。
- 隐藏与展开是同一投影计算；不叠加宿主高度动画、间距补偿或主滚动回写。

## 生命周期与性能

- start 幂等，body 晚到可以补启动；每个 document 同时仅有一个本插件控制器。
- flow/session 切换、HMR、服务断连后清理对应资源；原生隐藏状态和宿主后来写入的状态得到保留。
- React 替换同 key 节点时重绑；摘要被单独移除后恢复其入口，不依赖每秒审计。
- 结构按 seat 缓存，正文结构变化重建相关快照；普通工作文本更新只更新所在二级摘要。
- 先读取受影响组的可见性，再批量写入，避免长历史下读写交错。
- 稳定页面无周期性全文协调；后台使用一次性调度兜底。
- 异常时恢复原生内容，清理产生的 mutation 不形成无限重试；后续真实宿主变化可重新建立索引。

## 状态提示词与打包

- 保留既有 statusText 字段及默认 Deep sleeping...；空字符串恢复官方文案，用时后缀保留。
- 状态文字使用独立观察器；宿主原地更新也能替换，服务晚到/重连均生效。
- 设置保存期间禁止编辑，成功只清理该次提交的草稿；只读 scope 不允许提交。
- package/deploy 仅构建完整 tgz，不读凭据、不修改 profile、不停止或启动宿主。

## 验证

纯分组规则使用 Node 测试。DOM、CSS、真实 observer、原生组件生命周期、搜索、焦点、异步设置与几何通过 React/Chromium 验收。测试原生组件固定为 DSH 0.1.2-rc.1，并保留来源与 MIT 许可。完整后端部署另行验证。
